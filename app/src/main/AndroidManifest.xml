<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/eq_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/eq_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.EqualOpportuna"
        tools:targetApi="34" >
        <activity
            android:name=".activity_course_details"
            android:exported="false"
            android:label="@string/title_activity_course_detaisl"
            android:theme="@style/Theme.EqualOpportuna" />
        <activity
            android:name=".MainActivity2"
            android:exported="false"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.EqualOpportuna" />
        <activity
            android:name=".activity_course_detail"
            android:exported="true"
            android:label="@string/title_activity_course_detail"
            android:theme="@style/Theme.EqualOpportuna" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sign_up"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".login_page" />
    </application>

</manifest>